version: '3.8'

services:
  mysql:
    image: mysql:latest
    container_name: secfix
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: secfix
    ports:
      - "3310:3306"
    restart: always